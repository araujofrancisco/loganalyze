services:
  loganalyze:
    build: .
    ports:
      - "8081:8080"
    env_file:
      - .env
    environment:
      - TZ=America/Caracas
    volumes:
      - logdata:/data
    restart: unless-stopped

volumes:
  logdata:
